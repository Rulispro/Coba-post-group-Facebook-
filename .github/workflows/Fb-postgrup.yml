name: Facebook Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * *"

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate package-lock.json if missing
        run: |
          if [ ! -f package-lock.json ]; then
            echo "⚡ package-lock.json belum ada, generate dengan npm install..."
            npm install --legacy-peer-deps
          fi

      - name: Cache node modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        run: npm install --legacy-peer-deps

        - name: Download media from GitHub Release
        run: |
    mkdir -p media
    curl -L -o media/foto.jpg https://github.com/Rulispro/Coba-post-group-Facebook-/releases/download/v1.0/foto.jpg
    curl -L -o media/video.mp4 https://github.com/Rulispro/Coba-post-grouo-Facebook-/releases/download/v1.0/video.mp4


      - name: Run Puppeteer bot
        run:  node docs/index.js
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: true

      - name: Upload recording
        uses: actions/upload-artifact@v4
        with:
          name: recording-video
          path: recording.mp4

          - name: Clean media folder
            run: rm -rf media/*

